import 'package:flutter/material.dart';

class FifthSem extends StatelessWidget{
  @override
  Widget build(BuildContext context){
    return new Scaffold(
    appBar: new AppBar(
      title: new Text("Fifth Sem Notes"),
      backgroundColor: Colors.deepPurple,
    ),
    );
  }
}