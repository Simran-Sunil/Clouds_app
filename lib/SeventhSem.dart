import 'package:flutter/material.dart';

class SeventhSem extends StatelessWidget{
  @override
  Widget build(BuildContext context){
    return new Scaffold(
    appBar: new AppBar(
      title: new Text("Seventh Sem Notes"),
      backgroundColor: Colors.deepPurple,
    ),
    );
  }
}